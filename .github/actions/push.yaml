name: push
description: Create a custom action:v2 
inputs:
  input-name:
    description: "input-description"
    required: true
    default: "default-value"
runs:
  using: composite
  steps:
    - run: |
        echo "Hello World"
      shell: bash